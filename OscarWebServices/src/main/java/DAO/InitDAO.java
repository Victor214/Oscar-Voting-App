package DAO;

public class InitDAO {
    /*
        Usuario

        CREATE TABLE IF NOT EXISTS Usuario (
            id INT AUTO_INCREMENT PRIMARY KEY,
            login VARCHAR(200) NOT NULL UNIQUE,
            senha VARCHAR(200) NOT NULL,
            token TINYINT UNSIGNED,
            votoFilme int,
            votoDiretor int
        );
    */

}
